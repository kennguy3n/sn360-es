{
  "name": "sn360-addins-tests",
  "version": "1.1.0",
  "private": true,
  "description": "Test harness for the SN360 Outlook + Gmail add-ins (WS-7b).",
  "scripts": {
    "test": "node --test --test-reporter spec outlook/test/*.test.js gmail/test/*.test.js",
    "test:outlook": "node --test --test-reporter spec outlook/test/*.test.js",
    "test:gmail": "node --test --test-reporter spec gmail/test/*.test.js"
  },
  "devDependencies": {
    "office-addin-mock": "^2.4.0"
  },
  "engines": {
    "node": ">=18"
  }
}
