apiVersion: v1
kind: Service
metadata:
  name: sn360-es-encoder
  namespace: sn360-es
  labels:
    app.kubernetes.io/name: sn360-es-encoder
    app.kubernetes.io/component: tier1-inference
    app.kubernetes.io/part-of: sn360-es
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sn360-es-encoder
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
